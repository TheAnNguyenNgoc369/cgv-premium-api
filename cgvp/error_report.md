Đã rà soát ở chế độ chỉ đọc, không sửa code. Mình ưu tiên các lỗi có khả năng đụng production; các mục “Medium/Low confidence” là nơi cần test tải, multi-instance hoặc xác nhận cấu hình deploy để chốt tuyệt đối.

| ID | Severity | Confidence | Category | File | Line | Mô tả | Vì sao là vấn đề | Tình huống có thể xảy ra | Đề xuất hướng khắc phục |
| -- | -------- | ---------- | -------- | ---- | ---- | ----- | ---------------- | ------------------------ | ----------------------- |
| AUD-04 | High | Medium | Concurrency / Bug | [RefundService.cs](</c:/Users/nguyen ngoc the an/Downloads/cgvp/src/CinemaBooking.Application/Refunds/RefundService.cs:83>) | 83 | Refund kiểm tra trạng thái bằng “read then write”; `PaymentStatus.Refunded` check ở dòng 86 không bao giờ tới vì dòng 83 đã chặn mọi status khác `success`. | Không có conditional update/lock/unique refund per booking; 2 request refund song song có thể cùng đọc `success`, cùng cộng ví, tạo 2 refund. | User double-click refund hoặc retry qua network; nhiều instance xử lý cùng booking. | Dùng transaction với row lock/serializable hoặc conditional update `WHERE Status='success'`; thêm unique filtered constraint cho refund completed theo booking/payment. |
| AUD-05 | High | Medium | Concurrency / Bug | [PaymentService.cs](</c:/Users/nguyen ngoc the an/Downloads/cgvp/src/CinemaBooking.Application/Payments/PaymentService.cs:131>) | 131 | VNPay callback đọc `payment.Status`, rồi gọi `CompletePaymentAsync` không dùng update có điều kiện. | Hai callback thành công gần đồng thời có thể cùng vượt qua check pending; ticket/invoice tạo lặp và dựa vào unique constraint để nổ exception. | Gateway retry callback; người dùng refresh return URL; multi-instance xử lý cùng payment. | Áp dụng pattern như PayOS: `TryCompletePendingPaymentAsync`; chỉ finalize booking/ticket/invoice nếu update pending -> completed thành công. |
| AUD-06 | High | High | Security | [CheckInsController.cs](</c:/Users/nguyen ngoc the an/Downloads/cgvp/src/CinemaBooking.API/Controllers/CheckInsController.cs:150>) | 150 | `GET /api/checkins/history` cho Staff/Manager/Admin truyền tự do `staffId` và `cinemaId`; controller không scope theo user hiện tại. | Repository chỉ filter theo query request, không biết role/current user. Staff/manager có thể xem lịch sử check-in của rạp/nhân viên khác. | Staff gọi `/api/checkins/history?cinemaId=1` để xem dữ liệu liên rạp. | Truyền current user/role vào service; staff chỉ xem chính mình/cinema mình; manager chỉ cinema được gán; admin mới được query tự do. |
| AUD-07 | High | Medium | Scalability / Concurrency | [NotificationOutboxJob.cs](</c:/Users/nguyen ngoc the an/Downloads/cgvp/src/CinemaBooking.Infrastructure/BackgroundJobs/NotificationOutboxJob.cs:37>) | 37 | Background jobs lấy batch pending bằng query thường, không claim row bằng lock/status transition nguyên tử. | Khi chạy nhiều API instance, nhiều worker có thể xử lý cùng event/email. `AnyAsync` trước `Add` cũng không đủ nếu thiếu unique constraint DB. | Scale out 2 pods; cả hai cùng gửi email/notification cho một booking. | Claim event bằng `UPDATE ... OUTPUT`/row lock, trạng thái `processing` với lease, unique index cho notification event/user, idempotent email key. |
| AUD-08 | Medium | High | Performance / Validation | [CheckInHistoryRequest.cs](</c:/Users/nguyen ngoc the an/Downloads/cgvp/src/CinemaBooking.API/Contracts/CheckIns/CheckInHistoryRequest.cs:9>) | 9 | `Page`/`PageSize` không có validation min/max; repository dùng trực tiếp `Skip((page - 1) * pageSize).Take(pageSize)`. | PageSize rất lớn tạo query nặng, trả payload lớn; page âm có thể gây exception hoặc query không mong muốn. | Client gọi `pageSize=100000` làm DB/API chậm hoặc timeout. | Clamp/validate `page >= 1`, `pageSize 1..100` giống ActivityLog/ShowtimeType. |
| AUD-09 | Medium | High | Security / Maintainability | [AuthService.cs](</c:/Users/nguyen ngoc the an/Downloads/cgvp/src/CinemaBooking.Application/Authentication/AuthService.cs:463>) | 463 | Email verification/reset URL hardcode ngrok domain trong Application layer. | Production email có thể trỏ sai domain; token reset password bị gửi qua domain không kiểm soát hoặc hết hạn ngrok. | User bấm link reset ra domain dev; attacker chiếm/nghe domain tunnel cũ. | Đưa public frontend/base URL vào typed options theo environment; validate HTTPS/domain khi startup. |
| AUD-10 | Medium | Medium | Resource Leak / Scalability | [EmailQueueChannel.cs](</c:/Users/nguyen ngoc the an/Downloads/cgvp/src/CinemaBooking.Infrastructure/Email/EmailQueueChannel.cs:7>) | 7 | Channel email in-memory dùng `CreateUnbounded`. | Nếu producer nhanh hơn consumer hoặc SMTP chậm, memory có thể tăng không giới hạn. | Campaign/booking spike làm queue phình RAM, app recycle. | Dùng bounded channel với backpressure/drop policy rõ ràng; ưu tiên DB-backed queue đã có `EmailLogs` để bền hơn. |
| AUD-11 | Medium | Medium | Maintainability / Dependency | [CinemaBooking.API.csproj](</c:/Users/nguyen ngoc the an/Downloads/cgvp/src/CinemaBooking.API/CinemaBooking.API.csproj:9>) | 9 | Package Microsoft `9.0.6` đã cũ so với patch `9.0.17` hiện có cho dòng 9; Swashbuckle `10.2.1` cũng cũ hơn `10.2.3`. | Patch framework thường chứa bug/security fixes; không update định kỳ làm tăng rủi ro vận hành. NuGet hiện hiển thị JwtBearer/EF Core 9.0.17 và Swashbuckle 10.2.3. | Dính bug đã fix ở patch mới; build/deploy lệch runtime patch. | Chạy `dotnet list package --outdated`, nâng patch cùng major/minor, chạy test regression. Nguồn: NuGet JwtBearer/EF Core/Swashbuckle pages. |
| AUD-12 | Low | High | Code Smell / Exception Handling | [DependencyInjection.cs](</c:/Users/nguyen ngoc the an/Downloads/cgvp/src/CinemaBooking.API/DependencyInjection.cs:236>) | 236 | JWT auth failure ghi ra `Console.WriteLine`, không dùng structured logger. | Log khó truy vết, không có correlation/context và có thể lẫn vào stdout production. | Debug auth lỗi trên production khó lọc theo request/app instance. | Inject/use `ILogger` qua events hoặc custom handler; log Warning với event id, không log token/PII. |

Nguồn ngoài đã kiểm tra cho dependency: NuGet hiển thị `Microsoft.AspNetCore.Authentication.JwtBearer` latest stable `10.0.9` và dòng 9 có `9.0.17`; `Microsoft.EntityFrameworkCore.Design` cũng có `9.0.17`; `Swashbuckle.AspNetCore` hiện là `10.2.3`.  

Không chạy build/test vì yêu cầu là chỉ đọc và lập báo cáo.