Đã rà soát read-only toàn project, không sửa code. Build API xanh (`0 errors, 0 warnings`), NuGet runtime projects không có package vulnerable/deprecated; test project có `xunit 2.9.2` deprecated. Worktree trước audit đã có thay đổi sẵn ở `src/CinemaBooking.API/appsettings.json`, `src/CinemaBooking.Application/Notifications/BookingEmailTemplate.cs`, và `.claude/`.

| ID | Severity | Confidence | Category | File | Line | Mô tả | Vì sao là vấn đề | Tình huống có thể xảy ra | Đề xuất hướng khắc phục |
| -- | -------- | ---------- | -------- | ---- | ---- | ----- | ---------------- | ------------------------ | ----------------------- |
| AUD-001 | High | High | Security | [appsettings.json](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.API/appsettings.json:40) | 40-42 | PayOS `ClientId`, `ApiKey`, `ChecksumKey` đang nằm trong config repo. | Đây là credential thật hoặc có dạng credential thật; nếu file được push/deploy nhầm, khóa thanh toán bị lộ. | Người ngoài dùng key để gọi API PayOS, xác minh webhook giả hoặc tạo request gateway trái phép. | Rotate key, đưa toàn bộ secret sang environment/user-secrets/Key Vault, commit placeholder בלבד. |
| AUD-002 | High | High | Security | [Program.cs](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.API/Program.cs:31) | 31-32 | `KnownNetworks.Clear()` và `KnownProxies.Clear()` khiến app tin forwarded headers từ mọi nguồn. | Client trực tiếp có thể giả `X-Forwarded-For`/`X-Forwarded-Proto`, làm sai IP audit/rate limit/origin URL. | Kẻ tấn công spoof IP để né rate limit hoặc ghi audit log sai nguồn. | Chỉ cấu hình proxy/load balancer tin cậy; bỏ clear toàn bộ trong production. |
| AUD-003 | High | High | Security | [Program.cs](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.API/Program.cs:37) | 37-43, 73 | CORS global `AllowAll` cho mọi origin/header/method, dù config có `Frontend:AllowedOrigins`. | Mở surface không cần thiết cho browser clients; khó kiểm soát origin production. | Website lạ gọi public/auth endpoints từ trình duyệt người dùng, tăng rủi ro abuse và CSRF-like flows với token lưu phía FE. | Dùng policy đọc từ `Frontend:AllowedOrigins`, tách dev/prod, chỉ allow method/header cần thiết. |
| AUD-004 | Medium | High | Reliability | [Program.cs](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.API/Program.cs:51) | 51-55 | Seeder user chạy mỗi lần app startup, kể cả production. | Startup phụ thuộc DB write và logic seed; lỗi seed có thể làm app không lên hoặc ghi đè dữ liệu ngoài ý muốn. | Deploy nhiều instance cùng lúc gây race/lock, hoặc seed account mặc định xuất hiện lại sau khi bị đổi/xóa. | Chỉ chạy seed theo migration/admin job có kiểm soát hoặc gate bằng environment flag. |
| AUD-005 | Medium | High | Reliability | [DependencyInjection.cs](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.Infrastructure/DependencyInjection.cs:81) | 81-83 | `BackgroundServiceExceptionBehavior.Ignore` áp dụng toàn bộ hosted services. | Nếu job chết ngoài try/catch, host vẫn chạy nhưng chức năng nền im lặng mất. | Email, outbox, PayOS reconciliation hoặc seat expiration ngừng chạy sau một lỗi không bắt được. | Giữ service alive bằng loop-level catch cụ thể, health check/alert theo job, cân nhắc `StopHost` cho lỗi nghiêm trọng. |
| AUD-006 | Medium | Medium | Concurrency | [EmailDeliveryJob.cs](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.Infrastructure/BackgroundJobs/EmailDeliveryJob.cs:29) | 29-40 | Email job claim bằng query `ToListAsync` rồi set `processing`/`SaveChanges`, không dùng atomic update theo từng row. | Khi scale nhiều instance, hai worker có thể đọc cùng email trước khi một worker lưu trạng thái. | Khách nhận trùng email xác nhận/hoàn tiền; retry count và trạng thái giao hàng không phản ánh thực tế. | Claim bằng `ExecuteUpdate` có điều kiện status/lease như `NotificationOutboxJob`, hoặc dùng queue/row lock. |
| AUD-007 | Medium | High | Bug | [TicketRepository.cs](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.Infrastructure/Repositories/TicketRepository.cs:175) | 175-180 | Sau khi set ticket hiện tại `Used`, code gọi `AreAllTicketsUsedInBookingAsync` bằng query DB trước `SaveChanges`. | Query có thể chưa tính trạng thái tracked mới đổi của ticket hiện tại, nên booking cuối cùng không chuyển sang `used`. | Vé cuối cùng được check-in thành công nhưng booking vẫn `paid`, làm báo cáo/trạng thái lịch sử sai. | Tính `allTicketsUsed` trên tập ticket đã load/tracked hoặc save/update ticket trước rồi kiểm tra trong cùng transaction với lock. |
| AUD-008 | Medium | High | Validation | [DependencyInjection.cs](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.API/DependencyInjection.cs:108) | 108 | Fallback invalid model state trả `ValidationProblemDetails`, không theo envelope `{ success, message }`. | Repo convention yêu cầu response có `message` thì có `success`; nhiều endpoint sẽ trả shape khác nếu không nằm trong nhánh đặc biệt. | FE nhận lỗi validation raw, không parse được `success/message`, UX lỗi không nhất quán. | Chuẩn hóa fallback thành `{ success=false, message=... }`, thêm contract tests cho endpoint còn thiếu. |
| AUD-009 | Medium | Medium | Maintainability | [TicketRepository.cs](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.Infrastructure/Repositories/TicketRepository.cs:211) | 211-215 | Repository catch exception rồi `Console.WriteLine`, sau đó rethrow. | Mất structured logging/correlation, có thể leak message không chuẩn vào stdout. | Production log khó lọc theo request/job; lỗi check-in không có metadata đủ để điều tra. | Inject `ILogger<TicketRepository>` hoặc để upper layer/global handler log một lần có cấu trúc. |
| AUD-010 | Low | High | Logging | [Program.cs](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/src/CinemaBooking.API/Program.cs:8) | 8-17 | Startup dùng `Console.WriteLine` in environment. | Log không structured và không qua logging provider; nhỏ nhưng lệch convention production. | Log aggregation không có category/level; nhiễu khi scale nhiều instance. | Chuyển sang `ILogger` sau khi build app hoặc bỏ nếu không cần. |
| AUD-011 | Low | High | Dependency | [CinemaBooking.API.Tests.csproj](C:/Users/nguyen%20ngoc%20the%20an/Downloads/cgvp/tests/CinemaBooking.API.Tests/CinemaBooking.API.Tests.csproj:1) | package xunit | `dotnet list package --deprecated` báo test project dùng `xunit 2.9.2` deprecated, alternative `xunit.v3`. | Không ảnh hưởng runtime, nhưng tăng nợ bảo trì test stack. | Test tooling/adapter tương lai có cảnh báo hoặc chậm nâng cấp .NET. | Lên kế hoạch migrate xUnit v3 khi có thời gian, kiểm lại adapter/CI. |

**Validation đã chạy:** `dotnet build src\CinemaBooking.API\CinemaBooking.API.csproj --no-restore -o "$env:TEMP\cgvp-audit-verify-bin"` thành công; `dotnet list package --vulnerable --include-transitive` không có vulnerable package; `dotnet list package --deprecated` chỉ báo `xunit 2.9.2` ở test project.